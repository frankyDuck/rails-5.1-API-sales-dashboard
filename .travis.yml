language: ruby
rvm:
  - 2.4.1


services:
  - postgresql

before_script:
  - psql -c 'create database sales_app_test;' -U postgres
  - rake db:migrate

test:
  adapter: postgresql
  database: sales_app_test
  username: postgres

